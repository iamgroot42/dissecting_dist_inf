{
    "unlearning_config": {
        "learning_rate": 1e0,
        "stop_tol": 1e-4,
        "flip_weight_ratio": 0.002,
        "flip_tol": 1e-2,
        "max_iters": 100
    },
    "train_config": {
        "data_config": {
            "name": "new_census",
            "prop": "sex",
            "split": "adv",
            "squeeze": true,
            "value": 0.5
        },
        "epochs": 0,
        "learning_rate": 0,
        "batch_size": 0
    },
    "wb_config": {
        "attack": "permutation_invariant",
        "epochs": 200,
        "batch_size": 1000,
        "permutation_config": {
            "focus": "fc"
        }
    },
    "values": [
        0.0
    ],
    "num_models": 500,
    "victim_local_attack": true,
    "on_cpu": false
}