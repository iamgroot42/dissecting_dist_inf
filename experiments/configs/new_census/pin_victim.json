{
    "white_box": {
        "attack": "permutation_invariant",
        "epochs": 200,
        "batch_size": 1000,
        "train_sample": 450,
        "val_sample": 50,
        "save": false,
        "permutation_config": {
            "focus": "fc"
        }
    },
    "train_config": {
        "data_config": {
            "name": "new_census",
            "prop": "sex",
            "split": "adv",
            "squeeze": true,
            "value": 0.5
        },
        "epochs": 0,
        "learning_rate": 0,
        "batch_size": 0
    },
    "tries": 1,
    "values": [
        0.4
    ],
    "victim_local_attack": true,
    "num_victim_models": 500
}